services:
  capy-teacher-api:
    build:
      context: .
      dockerfile: Dockerfile.deploy
    ports:
      - "8000:8000"
    environment:
      REQUIRE_MODEL: "1"
      MODEL_DIR: "artifacts/onnx"
    restart: unless-stopped
